{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
      "config:base"
    ],
    "packageRules": [
      {
        "matchManagers": ["helm-requirements"],
        "matchDatasources": ["helm"],
        "matchDepTypes": ["helm"],
        "enabled": true
      }
    ],
    "helm-requirements": {
      "fileMatch": ["envs/talos/namespaces/.+/apps/.+/Chart\\.yaml$"]
    },
    "ignoreDeps": [],
    "separateMajorMinor": true,
    "separateMinorPatch": true,
    "prHourlyLimit": 0,
    "prConcurrentLimit": 0,
    "branchConcurrentLimit": 0,
    "hostRules": [
      {
        "matchHost": "chartmuseum.k8s-app.fredcorp.com",
        "username": "${HELM_USERNAME}",
        "password": "${HELM_PASSWORD}",
        "insecureRegistry": true
      }
    ],
    "commitMessageTopic": "helm release {{depName}}",
    "commitMessageExtra": "to {{#if isMajor}}v{{{newMajor}}}{{else}}{{#if isSingleVersion}}v{{{newVersion}}}{{else}}{{{newValue}}}{{/if}}{{/if}}",
    "prTitle": "chore(deps): update helm release {{depName}} to {{#if isMajor}}v{{{newMajor}}}{{else}}{{#if isSingleVersion}}v{{{newVersion}}}{{else}}{{{newValue}}}{{/if}}{{/if}} in {{replace 'envs/([^/]+)/namespaces/([^/]+)/apps/([^/]+)/.*' '$1/$2/$3' packageFileDir}}",
    "prBodyTemplate": "This PR updates the Helm release {{depName}} to version {{#if isMajor}}v{{{newMajor}}}{{else}}{{#if isSingleVersion}}v{{{newVersion}}}{{else}}{{{newValue}}}{{/if}}{{/if}}.\n\nEnvironment: {{replace 'envs/([^/]+)/.*' '$1' packageFileDir}}\nNamespace: {{replace 'envs/[^/]+/namespaces/([^/]+)/.*' '$1' packageFileDir}}\nApplication: {{replace 'envs/[^/]+/namespaces/[^/]+/apps/([^/]+)/.*' '$1' packageFileDir}}\n\n{{#if releaseNotes}}\n### Release Notes\n\n{{releaseNotes}}\n{{/if}}\n\n{{#if prBodyNotes}}\n{{{prBodyNotes}}}\n{{/if}}"
  }